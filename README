In this project, the contents of the folders are as follows:

scripts
	This contain the Python files that creates the Django model instances. 
	duration.py contain the R functions, that counts and describes the number of visits.

templates: This contains all the HTML files for the project.

dashboard:  This is the main folder, containing the models.py and views.py that executes the bulk of the processing.

emailusernames: This app enables the website to take email addresses as usernames.

Features to consider:

2. Where to put the MAC addresses.

3. More information on privacy.

4. What is done with the data, and who else it is supplied to, what form it is so done and why.

5. Set up production server.

